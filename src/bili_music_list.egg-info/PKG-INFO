Metadata-Version: 2.4
Name: bili-music-list
Version: 0.1.0
Summary: Export songs from a specified Bilibili favorites folder.
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: requests>=2.32.0

# Bilibili Favorites Music List

从零开始构建的 Python 项目：读取指定的 Bilibili 收藏夹，抓取其中所有视频，结合规则解析和可选 LLM 解析，输出一份歌曲清单。

## 功能

- 读取指定收藏夹 `media_id`
- 支持公开收藏夹，也支持通过 Cookie 访问私有收藏夹
- 拉取每个视频的标题、简介，默认不请求详情接口以降低风控
- 使用规则解析歌曲名/歌手
- 可选接入兼容 OpenAI `chat/completions` 的 LLM 做二次抽取
- 输出 `CSV` 或 `JSON`
- 自动按歌曲去重
- 可选自动创建网易云音乐歌单，并导入当前列表

## 项目结构

```text
src/bili_music_list/
  bilibili_client.py   # B 站收藏夹/视频详情抓取
  extractors.py        # 规则解析器
  llm_parser.py        # 可选 LLM 解析器
  netease_client.py    # 网易云歌单创建/搜索/导入
  cli.py               # 命令行入口
tests/
  test_extractors.py
  test_netease_client.py
```

## 安装

推荐先安装 Python 3.10+，然后执行：

```bash
pip install -e .
```

## 使用

### 1. 仅规则解析

```bash
bili-music-list --media-id 123456789 --parser heuristic --output output/music_list.csv
```

### 2. 规则 + LLM 混合解析

```bash
bili-music-list \
  --media-id 123456789 \
  --parser hybrid \
  --llm-base-url https://api.openai.com/v1 \
  --llm-api-key YOUR_API_KEY \
  --llm-model gpt-4.1-mini \
  --output output/music_list.csv

如果没有提供 LLM 参数，`hybrid` 会自动退回 `heuristic`。
```

### 3. 私有收藏夹

先把浏览器里的 Cookie 保存到 `cookie.txt`，再执行：

```bash
bili-music-list --media-id 123456789 --cookie-file cookie.txt
```

### 4. 可选详情抓取

默认不会逐个请求视频详情接口，因为该接口容易触发 `412` 风控。

如果你确认网络环境稳定、并且希望补充详情描述，可手动开启：

```bash
bili-music-list --media-id 123456789 --with-detail --output output/music_list.csv
```

### 5. 对现有 CSV 做 AI 二次解析

如果已经生成了 `output/music_list.csv`，可以继续用 AI 清洗歌曲名和歌手，并输出到另一个文件：

```bash
bili-music-list ^
  --input-csv output/music_list.csv ^
  --ai-output output/music_list_ai.csv ^
  --llm-base-url https://api.openai.com/v1 ^
  --llm-api-key YOUR_API_KEY ^
  --llm-model gpt-4.1-mini
```

输出仍然是一首歌一行，新增 `reason` 字段表示 AI 的清洗依据。

### 6. 自动创建网易云歌单并导入

项目支持把当前导出的歌曲列表，自动导入到一个**新建的网易云歌单**中。

前提：

- 你已经启动了一个兼容 `NeteaseCloudMusicApi` 的服务
- 你准备好了网易云登录 `Cookie`

示例：

```bash
bili-music-list ^
  --media-id 123456789 ^
  --output output/music_list.csv ^
  --netease-create-playlist "B站收藏夹导入" ^
  --netease-base-url http://127.0.0.1:3000 ^
  --netease-cookie-file netease_cookie.txt
```

导入完成后，工具会额外输出一个 JSON 报告，里面包含：

- 成功匹配并加入歌单的曲目
- 没有在网易云搜索到的曲目
- 新建歌单的 ID 和链接

### 7. 单独生成网易云登录二维码

如果你想把“生成二维码”和“实际导入”拆开，可以先单独生成二维码：

```bash
bili-music-list ^
  --netease-generate-login-qr ^
  --netease-base-url http://127.0.0.1:3000 ^
  --netease-qr-output output/netease_login_qr.png ^
  --netease-qr-key-file output/netease_qr_key.txt
```

扫码并在手机上确认后，再单独检查登录状态并导出 cookie：

```bash
bili-music-list ^
  --netease-check-login ^
  --netease-base-url http://127.0.0.1:3000 ^
  --netease-qr-key-file output/netease_qr_key.txt ^
  --netease-cookie-file output/netease_cookie.txt
```

当状态码为 `803` 时，表示登录成功，工具会把 cookie 写入文件，之后就可以直接拿这个 cookie 做歌单导入。

## 输出字段

默认 CSV 为“每首歌一行”的简洁格式：

- `song_title`: 歌曲名
- `artist`: 歌手
- `confidence`: 置信度
- `source_bvid`: 来源视频 BV 号
- `source_video_title`: 来源视频标题
- `source_video_url`: 来源视频链接
- `uploader`: 来源 UP 主

## 已验证的接口

- 收藏夹列表：`https://api.bilibili.com/x/v3/fav/resource/list`
- 视频详情：`https://api.bilibili.com/x/web-interface/view`
- 网易云搜索：兼容 `NeteaseCloudMusicApi` 的 `/cloudsearch`
- 网易云建歌单：兼容 `NeteaseCloudMusicApi` 的 `/playlist/create`
- 网易云加歌：兼容 `NeteaseCloudMusicApi` 的 `/playlist/tracks`

## 后续可扩展

- 接入 Bilibili 官方登录态管理
- 增加批量收藏夹处理
- 增加歌名标准化和第三方音乐平台匹配
- 为 LLM 增加本地缓存，减少重复请求
